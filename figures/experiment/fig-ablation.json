{
  "meta": {
    "id": "fig:ablation",
    "kind": "experiment",
    "title": "Ablation, at 1.2% uplink loss so the protocol components are exercised",
    "source": "E-4, LIBERO success rate with each component removed",
    "placeholder": true,
    "notes": "Row order is the `y` column (1 = bottom); the tick label for row k is label `a<k>`. The two purple rows are the protocol ablations: they lose success WITHOUT saving bytes, which is the signature of a correctness bug rather than a trade-off -- so the uplinkmb figures below are load-bearing for the argument even though they are not plotted. They are quoted in the body prose of the ablation subsection (the caption is kept short per the format review). Keep them in the same row order.",
    "uplinkmb": [4.0, 3.4, 6.9, 5.8, 7.1, 10.3, 4.2, 3.8],
    "uplinkmb_note": "MB per episode for rows 1..8, quoted in the body text but not drawn"
  },
  "scalars": {
    "sysline": 78.9
  },
  "labels": {
    "a1": "no seq.\\ numbers",
    "a2": "no forced resync",
    "a3": "no action cond.",
    "a4": "static $\\tau$",
    "a5": "WM-latent trigger",
    "a6": "fidelity trigger",
    "a7": "\\sys",
    "a8": "oracle divergence"
  },
  "series": {
    "protocol": {
      "columns": ["succ", "y"],
      "rows": [[61.2, 1], [68.9, 2]]
    },
    "trigger": {
      "columns": ["succ", "y"],
      "rows": [[71.3, 3], [74.6, 4], [76.4, 5], [78.2, 6]]
    },
    "sys": {
      "columns": ["succ", "y"],
      "rows": [[78.9, 7]]
    },
    "oracle": {
      "columns": ["succ", "y"],
      "rows": [[79.3, 8]]
    }
  }
}
