# generated from the JSON by build.py; never edited, never committed
.build/

# latexmk intermediates from the standalone figure builds
*.aux
*.log
*.fls
*.fdb_latexmk
*.out
*.synctex.gz

# the figure PDFs ARE committed -- main.tex includes them and must build
# without a TeX-plus-Python toolchain.
!*.pdf
